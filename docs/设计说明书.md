# 心理咨询师考试背题系统——设计说明书

> **文档版本**：v2.5
> **更新日期**：2026-09-24
> **项目名称**：心理咨询师考试背题系统
> **对应需求文档**：需求说明书 v2.5

---

## 1. 系统架构设计

### 1.1 四层架构

系统采用四层分层架构，各层职责清晰、边界明确：

```
┌────────────────────────────────────────────────────────────────────┐
│                    展示层 (Presentation Layer)                    │
│  app.py ── 主控制器、侧边栏导航、全局CSS注入、身份解析、管理员判定 │
│  pages/statistics.py     ── 首页（五维统计+相关性分析）          │
│  pages/specialized.py   ── 专项训练（含草稿保存/恢复）          │
│  pages/mock_exam.py    ── 模拟考试（两科定时+草稿）            │
│  pages/consolidation.py── 巩固练习（四层优先级抽题）             │
│  pages/wrongbook.py    ── 错题本（双模式）                    │
│  pages/mnemonic.py     ── 助记助学（数字/人名/量表速查）      │
│  pages/settings.py     ── 配置管理（题库按角色分区/备份/参数） │
├────────────────────────────────────────────────────────────────────┤
│                    业务逻辑层 (Business Logic Layer)               │
│  utils/data_manager.py —— 业务门面（按 user_id 路由 + 缓存）    │
│  utils/account_manager.py —— 身份（管理员/设备绑定/密码）       │
│  · 抽题算法：_priority_sample / _balanced_sample_by_type        │
│  · 掌握度计算：_recalc_mastery_fields                         │
│  · 错题规则：add_wrong_record / add_correct_record             │
│  · 批量I/O：batch_update_wrong_and_stats / batch_add_answer_  │
│  · 用户上下文：set_current_user（幂等，每帧校正）              │
│  · 缓存管理：per-rerun 缓存 + _data_version 失效机制          │
├────────────────────────────────────────────────────────────────────┤
│                    数据访问层 (Data Access Layer)                  │
│  utils/data_access.py —— SQLite DAO（单库多租户，进程内单例）   │
│  utils/parser.py      —— docx解析（只算内容指纹，不分配 ID）     │
│  · 建表逻辑：_init_db（含旧库守卫 _assert_schema_ready）      │
│  · 事务保护：批量操作使用单事务连接                            │
│  · 连接分两级：_get_conn（读写+WAL）/ _get_light_conn（轻读）   │
│  · 导入配对：_match_questions（内容指纹 → 位置 → 文本相似度）   │
├────────────────────────────────────────────────────────────────────┤
│                    数据存储层 (Storage Layer)                     │
│  data/exmsys.db  —— **唯一** SQLite数据库（14张表，单库多租户） │
│  exmbase/         —— 原始docx题库文件                         │
│  data/backup/     —— 自动备份目录                             │
│  data/archive/    —— 改造前旧库归档（程序不读）                │
└────────────────────────────────────────────────────────────────────┘
```

### 1.2 分层职责说明

| 层级 | 职责 | 禁止操作 |
|------|------|---------|
| 展示层 | 用户交互、参数收集、结果展示 | 禁止直接操作数据库 |
| 业务逻辑层 | 抽题算法、成绩计算、错题规则、缓存管理、用户上下文、身份判定 | 禁止直接写SQL |
| 数据访问层 | SQL执行、事务管理、per-rerun缓存、owner/user 隔离 | 禁止包含业务逻辑 |
| 数据存储层 | 数据持久化 | — |

**v2.5 关键设计约束（改动前必读）**

1. **唯一数据库**：全系统只有一个 `data/exmsys.db`。任何"切库"逻辑都不应再出现；
   用户隔离一律靠 SQL 条件里的 `user_id` / `owner_id`。
2. **题目 ID 一经分配永不变更**。这是"管理员改题库不切断用户历史"的基础，
   也是"用户题库与初始题库通过 ID 关联"的落地方式。任何会让 ID 漂移的改动都是缺陷。
3. **`_current_user_id` 是模块级全局变量**，Streamlit 多 session 共享进程会互相覆盖，
   因此 app.py 必须**每一帧**调用 `set_current_user()` 校正（该函数幂等，无额外开销）。
4. **作用域不能用 `<div class='wrap'>` 包裹来限定**：markdown / columns / button
   各自是独立 React 组件，那个 div 是封闭空 div，后续元素是它的兄弟节点。
   限定作用域必须用真实容器 testid（`[data-testid="stSidebar"]`、`[data-testid="stMain"]`）
   或 `st.container(key=...)` 生成的 `class="st-key-<key>"`。

---

## 2. 数据访问层设计（DAO）

### 2.1 单例 + 用户上下文

```python
_DAO_SINGLETON = None

def get_data_access(db_path=None, user_id=None) -> DataAccess:
    global _DAO_SINGLETON
    if _DAO_SINGLETON is None:
        _DAO_SINGLETON = SQLiteDataAccess(db_path=db_path, user_id=user_id)
    elif user_id is not None:
        _DAO_SINGLETON.set_user_id(user_id)   # 幂等同步用户上下文
    return _DAO_SINGLETON

def reset_data_access():                      # 测试或库切换时丢弃单例
    global _DAO_SINGLETON
    _DAO_SINGLETON = None
```

`SQLiteDataAccess` 内的上下文解析：

```python
def _uid(self, user_id=None):     # 生效的 user_id：显式传入优先，否则用当前上下文
    return user_id if user_id else self.user_id

def _owner(self, owner_id=None):  # 生效的 owner：显式传入优先，否则写自己的私人题库
    return owner_id if owner_id else self.user_id
```

### 2.2 连接分两级

```python
def _get_conn(self):          # 读写连接：WAL + 同步/缓存调优
    conn = sqlite3.connect(self.db_path, timeout=10)
    conn.row_factory = sqlite3.Row
    conn.execute("PRAGMA journal_mode=WAL")
    conn.execute("PRAGMA synchronous=NORMAL")
    conn.execute("PRAGMA cache_size=-4000")
    return conn

def _get_light_conn(self):    # 轻量连接：不做任何 PRAGMA，仅供"每帧读一眼"的小查询
    conn = sqlite3.connect(self.db_path, timeout=10)
    conn.row_factory = sqlite3.Row
    return conn
```

**为什么要分两级**：`PRAGMA journal_mode=WAL` 需要取写锁，代价远高于一次普通 SELECT。
改造前 `get_questions_version()` 每帧都走 `_get_conn()`，实测约 **259ms/次**，
是当时"勾选答案卡顿"的头号元凶。现在该函数走 `_get_light_conn()`。

### 2.3 建表与旧库守卫

`_init_db()` 在 DAO 首次初始化时执行：

1. 创建 `questions` / `case_studies`（带 `owner_id`）
2. **调用 `_assert_schema_ready()` 做旧库守卫**
3. 创建唯一索引 `uniq_questions_owner_md5(owner_id, md5)`
4. 创建 7 张用户数据表（带 `user_id`）、`app_config` / `user_config`、`users` / `user_devices`
5. 确保题库版本号记录存在（`app_config._questions_data_version`）

**旧库守卫的规则（曾经写错过，必须保持）**：

```python
def _assert_schema_ready(cur):
    # ① questions 表已存在但缺 owner_id → 是改造前的旧库，明确报错并指引迁移
    # ② question_stats 表**不存在时放行**
    #    （本方法在"建表过程中"被调用，早于 question_stats 建表；
    #      全新库的正常路径就是先无表、后建表。把"表不存在"当旧库，
    #      会让全新库（云端首次部署）直接起不来 —— 这是已被回归测试锁住的 P0）
```

### 2.4 事务保护

批量操作使用独立连接 + 单事务，确保原子性：

```python
def batch_update_wrong_and_stats(self, wrong_qids, correct_qids, stats_updates,
                                 uncertain_map=None, exam_record=None,
                                 exam_type=None, user_id=None) -> None:
    # 1. 构建 affected_qids（只碰受影响的题，不做全表扫描）
    # 2. 读取受影响题的现有统计（1 次读）
    # 3. 同一事务内批量更新 wrong_questions + question_stats（1 次写）
    # 4. 可选：同一事务内写入考试记录（省掉一次连接开销）
```

### 2.5 per-rerun 缓存

`_rerun_cache_questions` / `_rerun_cache_stats` 缓存本次 rerun 内的查询结果。
每个缓存都记录其归属的 `user_id`（`*_user`），读取时校验一致才复用 ——
因为这些是模块级全局变量，多 session 会互相覆盖，不校验就会串号读到别人的数据。
每次 rerun 开始时由 `invalidate_rerun_cache()` 清空。

---

## 3. 核心业务算法设计

### 3.1 优先级抽题算法

**目标**：让用户优先看到最薄弱的题目。

**四层排序**（关联系数计算后用 `_priority_sample` 执行）：

| 排序键 | 说明 |
|---------|------|
| `wrong_count - correct_count` 降序 | 答错多于答对的题优先 |
| 答错过(0) > 从未答过(1) > 答对过(2) | 副排序 |
| `wrong_count` 降序 | 同层级内高频错题优先 |
| 随机 | 最终平局打破 |

### 3.2 均衡采样算法

**目标**：确保超类下各子板块均匀出题，避免大板块垄断。

**算法步骤**（`_balanced_sample_by_type`）：
1. 计算基础配额：`base = total // n`
2. 前 `extra` 个类别多分1道（`extra = total % n`）
3. 各子板块内部按优先级排序后取前 N 道
4. 配额未用完时从剩余题目中按优先级补齐

### 3.3 掌握度计算算法

**目标**：基于历史答题记录，计算每道题的掌握度等级（0-5级）。

**输入**：该题所有历史答题记录（answer_records 表）

**计算步骤**（`_recalc_mastery_fields`）：
1. 统计 `correct_count`、`wrong_count`
2. 计算正确率 `accuracy = correct_count / (correct_count + wrong_count)`
3. 映射正确率到掌握度等级：

| 正确率范围 | 等级 | 说明 |
|-----------|------|------|
| < 20% | 0级 | 未掌握 |
| 20%-40% | 1级 | 薄弱 |
| 40%-60% | 2级 | 入门 |
| 60%-80% | 3级 | 良好 |
| 80%-95% | 4级 | 熟练 |
| ≥ 95% | 5级 | 精通 |

4. 计算置信度：答题次数越多置信度越高（`confidence = min(1.0, (correct_count + wrong_count) / 10)`）
5. 检测不稳定标记：
   - **消退型**：近期正确率明显低于远期 → `unstable = 1, retention_due = 计算值`
   - **波动型**：正确/答错交替出现 → `unstable = 1`

### 3.4 错题入出本规则

**入本条件**（满足以下所有条件）：
- `wrong_count >= correct_count`
- `wrong_count > 0` 或 `correct_count > 0`（至少答过一次）

**出本条件**（满足以下任一条件）：
- `correct_count > wrong_count`（答对次数严格大于答错次数）
- 用户手动点击"我已掌握本题"且 `correct_count > wrong_count`

---

## 4. 缓存与性能设计

### 4.1 三级缓存体系

| 缓存层级 | 存储位置 | 失效时机 | 用途 |
|----------|----------|---------|------|
| per-rerun缓存 | `data_access.py` 模块级字典 | 每次 rerun 开始（`invalidate_rerun_cache()`） | 避免同次 rerun 多次查询 DAO |
| session_state缓存 | `st.session_state._stats_cache` 等 | `_data_version` 变化时 | 避免跨 rerun 重复读取数据库 |
| 版本号失效 | `st.session_state._data_version` | 每次数据写入后 `+1` | 触发 session_state 缓存失效 |

### 4.2 批量I/O设计

**优化前**（v2.1及之前）：每题依次调用 `add_wrong_record` / `add_correct_record` / `add_answer_record`，每次调用执行一次数据库读写。

**优化后**（v2.2及之后）：
1. 提交时先收集所有操作数据（wrong_qids / correct_qids / stats_updates / answer_records）
2. 调用 `batch_update_wrong_and_stats()` 一次性更新错题库和答题统计（1次事务）
3. 调用 `batch_add_answer_records()` 一次性追加答题记录（1次事务）

**效果**：250题提交从 ~500+ 次 I/O 降至 ~4 次 I/O，性能提升近百倍。

### 4.3 首页统计缓存

`statistics.py` 中的 `_get_cached_stats()` 函数：
1. 检查缓存版本号是否等于 `_data_version`
2. 命中：直接返回缓存数据（0次 I/O）
3. 未命中：从数据库加载6类数据，存入缓存（6次 I/O）

---

## 5. 数据库设计（SQLite 单库多租户）

> **v2.5 结构**：全系统一个 `data/exmsys.db`，共 14 张表。
> 题库用 `owner_id` 分层，用户数据用 `user_id` 隔离。

### 5.1 表关系图

```
questions (题目主表，owner_id 分层：'__public__' | '<user_id>')
    ├── 1:N ──> answer_records       (答题记录，user_id)
    ├── 1:N ──> question_stats       (答题统计，PK=(question_id,user_id))
    ├── 1:N ──> wrong_questions      (错题库，  PK=(question_id,user_id))
    └── 1:N ──> case_studies         (案例背景，owner_id 同套分层)

exam_records        (考试记录，user_id)
mock_exam_records   (模拟考试记录，user_id)
study_records       (训练会话记录，PK=session_id + user_id)
drafts              (草稿，PK={user_id}|{prefix}|{draft_id})
app_config          (全局配置，如题库版本号)
user_config         (每用户配置，PK=(user_id,key))
users               (身份：user_id / username / password_hash / role / …)
user_devices        (设备绑定：device_fp → user_id)
```

### 5.2 核心表结构

**questions 表**（题目主表）：

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | TEXT | PK | 题目唯一ID（12位）。**永不变更** |
| owner_id | TEXT | NOT NULL, DEFAULT '__public__' | `'__public__'` 或 `<user_id>` |
| source_file | TEXT | | 来源docx文件名 |
| index_num | INTEGER | | 题目在源文件中的序号 |
| type | TEXT | NOT NULL | 题型（single/multi/judge/indefinite/案例题） |
| question | TEXT | NOT NULL | 题目文本 |
| options | TEXT | NOT NULL | 选项字典（JSON字符串） |
| answer | TEXT | NOT NULL | 正确答案 |
| explanation | TEXT | | 解析内容 |
| md5 | TEXT | NOT NULL | 内容指纹（UNIQUE 在 owner 作用域内） |
| category | TEXT | | 知识板块名称 |
| exam_type | TEXT | | 题库类型 |
| case_study_id | TEXT | | 案例题背景ID |
| is_case_background | INTEGER | DEFAULT 0 | 是否案例背景题 |

索引：`idx_questions_owner` / `idx_questions_exam_type` / `idx_questions_category` /
`idx_questions_type` / `idx_questions_case`，
唯一索引 `uniq_questions_owner_md5(owner_id, md5)`。

> **公共 owner 为什么用 `'__public__'` 而不是 NULL**：SQLite 中 NULL 互不相等，
> `UNIQUE(owner_id, md5)` 会因此失效（公共题库去重直接坏掉）。

**question_stats 表**（答题统计）：

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| question_id | TEXT | **PK** | 题目ID（关联 questions.id） |
| user_id | TEXT | **PK** | 数据归属用户（复合主键） |
| exam_type | TEXT | | 题库类型 |
| correct_count | INTEGER | DEFAULT 0 | 累计答对次数 |
| wrong_count | INTEGER | DEFAULT 0 | 累计答错次数 |
| last_answer_time | TEXT | | 最后一次答题时间（ISO格式） |
| last_correct | INTEGER | | 最后一次答题是否正确（0/1） |
| mastery_level | INTEGER | DEFAULT 0 | 掌握度等级（0-5） |
| confidence | REAL | DEFAULT 0.0 | 掌握度置信度（0.0-1.0） |
| self_uncertainty | REAL | DEFAULT 0.0 | 自我不确定度 |
| unstable | INTEGER | DEFAULT 0 | 是否不稳定（0/1） |
| first_answer_time | TEXT | | 首次答题时间 |

**wrong_questions 表**（错题库）：

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| question_id | TEXT | **PK** | 题目ID（关联 questions.id） |
| user_id | TEXT | **PK** | 数据归属用户（复合主键） |
| exam_type | TEXT | | 题库类型 |

**drafts 表**（草稿保存）：

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | TEXT | PK | `{user_id}|{prefix}|{draft_id}` —— **带 user_id 是为了防不同用户同名草稿互相覆盖** |
| user_id | TEXT | NOT NULL | 数据归属用户 |
| prefix | TEXT | NOT NULL | 草稿前缀（study/spec/mock/…） |
| draft_id | TEXT | NOT NULL | 草稿ID（会话或板块标识） |
| data | TEXT | NOT NULL | 草稿数据（JSON字符串） |
| saved_at | TEXT | NOT NULL | 保存时间（ISO格式） |
| exam_type | TEXT | | 题库类型 |

**users / user_devices 表**（身份，v2.5 新增）：

| users | 类型 | 约束 | 说明 |
|-------|------|------|------|
| user_id | TEXT | PK | `md5("exmsys_device_"+指纹)[:12]` 或管理员固定值 |
| username | TEXT | UNIQUE | 账号名（可为 NULL：纯设备用户） |
| password_hash | TEXT | | SHA-256 |
| role | TEXT | NOT NULL, DEFAULT 'user' | `'user'` / `'admin'` |
| display_name | TEXT | | 显示名 |
| created_at / last_login | TEXT | | 时间戳 |

| user_devices | 类型 | 约束 | 说明 |
|--------------|------|------|------|
| device_fp | TEXT | PK | 设备指纹（`md5(user_agent)[:32]`） |
| user_id | TEXT | NOT NULL | 归属用户 |
| first_seen / last_seen | TEXT | | 时间戳 |

### 5.3 题目 ID 分配与导入配对（本题库最关键的两段逻辑）

**ID 分配**（`make_question_id`）：

```python
def make_question_id(owner_id, q) -> str:
    return md5(f"{owner_id}|{题干}{str(sorted(选项.items()))}{答案}")[:12]
```

- 把 `owner_id` 纳入哈希 → "用户私题"与"公共题"即使内容相同也得到不同 ID，互不干扰；
- 插入前若 ID 已存在，再加盐重算，保证不覆盖既有题；
- **ID 一经分配永不变更** —— 这是用户历史零断链的前提。

**内容指纹**（`question_md5` / `_content_fp`）：

```python
md5(题干 + str(sorted(选项.items())) + 答案)
```

必须**两侧现算**（新题算一次、库中行也算一次），不能拿"新题算出的值"去比
"库里存的 `md5` 列" —— 因为管理员直接改库改内容后该列不会同步，本题库里就有
**2417/5118 条**是这样的历史遗留。拿它当主键会大面积漏配，导致重新导入时给旧题换新 ID、
把用户历史打断（这正是本设计要防的事故）。

**三级配对**（`_match_questions`）：

| 级别 | 信号 | 适用 |
|------|------|------|
| 1 | 内容指纹（两侧现算）相同；退回库中 `md5` 列 | 内容未变 |
| 2 | `(source_file, index_num, type)` 相同 | 题号可靠时 |
| 3 | 同一 `source_file` 内题干文本相似度最高且 ≥ 0.60 | 题干被改过 |

- 级别 2 **不能单独依赖**：本题库中 `(source_file, index_num)` 并不唯一
  （曾有文件 500 题里 250 题 `index_num=0`；另一文件 220 题全部 #0；99 题 `source_file` 为 NULL）
- 级别 3 是 `O(n·m)` 比较，设 **20 万次比较预算** + `real_quick_ratio()` / `quick_ratio()` 预筛；
  超预算就停止模糊配对、剩余按新题插入（宁可多几道新题，也不让导入卡死）
- `used_ids`（只增不减，防新题 ID 撞车）与 `matched_ids`（`replace` 模式据此判定删除）**是两个独立集合**，
  混用会导致 `replace` 永远删不掉东西

**md5 唯一索引的防撞保护**：更新已匹配题时，若新 `md5` 已被同 owner 的另一题占用
（本题库有 1237 组内容重复的题），则**保留本行原 md5** 并记入导入日志，
绝不触发 `UNIQUE(owner_id, md5)` 冲突。

---

## 6. 页面路由与状态机设计

### 6.1 页面路由

`app.py` 使用自定义侧边栏导航（隐藏 Streamlit 默认多页面导航），路由逻辑：

```python
menu_items = {
    "首页": "🏠",
    "专项训练": "🎯",
    "模拟考试": "📝",
    "巩固练习": "🔁",
    "错题本": "📕",
    "助记助学": "🧠",
    "配置管理": "⚙️",
}
```

选中后通过 `import` 动态加载对应页面模块，避免启动时加载全部页面。

### 6.2 各页面状态机

**专项训练（specialized.py）**：

```
idle ──► running ──► submitted
  │                      │
  │                      └──► idle (再做一次/返回)
  └──► idle (返回首页)
```

- `idle`：开始界面（选择板块/恢复草稿）
- `running`：答题进行中（可保存草稿）
- `submitted`：提交后成绩报告

**模拟考试（mock_exam.py）**：

```
idle ──► psychology ──► psychology_finished
                              │
                              └──► counseling ──► counseling_finished
                                                                 │
                                                                 └──► all_finished
```

- 两科独立计时、独立提交
- 支持草稿保存与恢复

**巩固练习（consolidation.py）**：

```
idle ──► running ──► submitted
```

- 与专项训练类似，但题目来源为"不稳固题目"

**错题本（wrongbook.py）**：

```
解析模式：idle（左侧列表 + 右侧详情）
做错题模式：idle ──► running ──► submitted
```

---

## 7. 草稿系统设计

### 7.1 草稿数据结构

存储于 `drafts` 表，`data` 字段为 JSON 字符串：

```json
{
  "draft_id": "spec_abc123",
  "draft_type": "spec",
  "exam_type": "心理学会咨询师四级",
  "category": "心理学导论",
  "mode": "specialized",
  "question_ids": ["xxx", "yyy", ...],
  "answers": {"xxx": "A", "yyy": ["A", "B"], ...},
  "submitted": false,
  "saved_at": "2026-06-28T19:00:00.000000"
}
```

### 7.2 草稿保存与恢复流程

**保存**：
1. 用户在答题过程中点击"保存草稿"
2. 收集当前 `question_ids`、`answers`、当前进度
3. 调用 `save_draft(draft_type, data)` 写入 `drafts` 表
4. 提示"草稿已保存"并返回开始界面

**恢复**：
1. 开始界面加载该 `draft_type` 的所有草稿
2. 用户点击"继续作答"
3. 从 `drafts` 表读取草稿数据
4. 恢复 `question_ids`、`answers`、当前进度
5. 进入 `running` 状态继续答题

---

## 8. 部署与运行

### 8.1 运行环境

| 组件 | 版本要求 | 说明 |
|------|---------|------|
| Python | 3.14 | 开发语言 |
| Streamlit | 1.57+ | Web框架 |
| python-docx | 1.1.0+ | docx解析 |
| SQLite | 3.x | 数据存储（内置） |

### 8.2 启动方式

**方式一**：双击 `start.bat`
```batch
streamlit run app.py --server.port 8511
```

**方式二**：命令行
```bash
cd E:\LingMa\psych-exm\exmsys-dev
streamlit run app.py --server.port 8511
```

### 8.3 数据目录与目录权限

数据目录默认 = 工程 `data/`，可用环境变量覆盖：

```bash
export EXMSYS_DATA_DIR=/mnt/exmsys-data    # 云端挂载卷 / 测试副本
```

（`utils/data_access.py` 与 `utils/data_manager.py` 都读该变量）

| 目录 | 权限要求 | 说明 |
|------|---------|------|
| `data/` | 读写 | 唯一 SQLite 数据库文件 + backup/archive |
| `exmbase/` | 只读 | 原始docx题库 |
| `docs/` | 只读 | 文档文件 |
| `scripts/` | 只读（迁移时需写 data/） | 运维与测试脚本 |

---

## 9. 测试与验收

### 9.1 测试脚本清单

| 脚本 | 覆盖 | 安全机制 |
|------|------|---------|
| `scripts/test_multiuser.py` | 14 项：空库初始化 / user_id 派生不变 / 公共与私人题库可见性 / 个人数据隔离 / 同名草稿 / **重复导入 ID 零漂移** / **md5 陈旧仍复用原 ID** / 改解析原地更新 / 错题不断链 / 清空只影响自己 / 设备识别 / 版本号递增 | 自建临时库，并在收尾比对真实库指纹与目录清单 |
| `scripts/test_app_smoke.py` | 8 项：AppTest 端到端；无「题库为空」；连续 rerun 稳定；**遍历 pages/ 全部 9 个页面渲染无异常**；管理员可查；题库归属与计数自洽；users/user_devices 可用 | 自动复制真实库到临时目录并改道 `EXMSYS_DATA_DIR`，收尾比对 sha256 与体积 |
| `scripts/test_empty_warning.py` | 「题库为空」误报回归（初始 + 连续 10 次 rerun） | 同上（`scripts/_testenv.py`） |
| `scripts/test_switch_exam.py` | 题库切换后 questions 正确重载 | 同上 |
| `scripts/verify_single_db_migration.py` | 迁移**逐行**内容校验（29 项）：题目 ID 一一对应 + 逐字段一致、md5 逐字保留、用户表逐行指纹、配置逐项、身份外键、管理员、复合主键、唯一索引、交叉污染 | 源库以 `mode=ro` 打开，只读 |

**运行方式**：直接 `python scripts/<脚本名>.py`。AppTest 类脚本会**自己**把数据目录
改道到真实库的副本（`scripts/_testenv.py`），全程只读真实库并在结束时校验指纹 —— 
因为 AppTest 会真的执行 app.py，而 app.py 会"按设备指纹自动建用户"，直接跑真实库就是污染生产数据。

### 9.2 单库改造的验收标准（已全部达成）

| 项 | 标准 | 结果 |
|----|------|------|
| 题目内容 | 5118 道题 ID 一一对应、逐字段一致 | ✅ |
| 题目 ID | 公共题库迁移后原样保留（零断链） | ✅ |
| 用户数据 | 17908 答题记录 / 5109 统计 / 21 错题 / 135 考试 / 30 模拟 / 21 背题 / 3 草稿 全部逐行一致 | ✅ |
| 配置 | 5 个用户 × 20 项配置全部迁入 `user_config` 且值一致 | ✅ |
| 身份 | 5 条设备映射、1 个账号（用户名+密码哈希）入库 | ✅ |
| 管理员 | `admin`/`admin`，role=admin，密码哈希 = sha256('admin') | ✅ |
| 结构 | 复合主键、唯一索引、drafts.user_id 齐备 | ✅ |
| 端到端 | 应用可启动、9 个页面渲染无异常、5118 题可加载 | ✅ |
| 代码健康 | 25 个 Python 文件语法通过、全部 CRLF | ✅ |

---

## 10. 已知技术债与改进方向

### 10.1 P0 级别（功能瘫痪）

| 问题 | 位置 | 状态 |
|------|------|------|
| 函数定义丢失 | `pages/specialized.py` | ✅ 已修复（v2.4 后） |
| **全新库无法初始化** | `utils/data_access.py` `_assert_schema_ready()` 在 `question_stats` 建表前就校验 → 空库必然抛错，云端首次部署直接起不来 | ✅ 已修复（v2.5），并由 `test_multiuser.py` 第 1 项锁住 |

### 10.2 P1 级别（数据风险）

| 问题 | 位置 | 说明 | 状态 |
|------|------|------|------|
| 题库 md5 列陈旧 | `questions.md5` | 2417/5118 条与内容对不上（历史遗留）；库内还有 1237 组"内容重复、ID 不同"的题，因此**不能**全量重算（会撞 `UNIQUE(owner_id,md5)`） | 已通过"配对改为两侧现算内容指纹"绕开；`refresh_question_md5.py` 已加警示 |
| md5 列职责 | `questions.md5` | 唯一索引 `uniq_questions_owner_md5` 仍在用它，故必须保持"同 owner 内不重复"，导入更新时已加防撞保护 | 已处理 |
| 事务不一致 | `utils/data_manager.py` `add_correct_record()` | 直接操作 `save_question_stats`，绕过 DAO 的批量事务 | 待修 |
| 内容重复题 | `questions` | 1237 组（2474 题）题干+选项+答案完全相同但 ID 不同，需要管理员决定是否合并 | 待人工处理 |

### 10.3 P2 级别（文档/体验）

| 问题 | 位置 | 说明 | 状态 |
|------|------|------|------|
| 文档过时 | `docs/需求说明书.md`、`docs/设计说明书.md` | 描述 JSON 存储 / 每用户一个库 | ✅ v2.5 已重写相关章节 |
| 模拟考试题数固定 | `utils/data_manager.py` `MOCK_EXAM_CONFIG` | 两科题数硬编码，未暴露到系统设置 | 待改 |
| 默认题库选择 | `app.py` 取 `get_available_exam_types()[0]` | 按 Unicode 排序取第一个，当前默认落到"心理协会咨询师初级"(444 题)，不一定符合用户预期 | 待确认是否改为可配置 |
| 旧库残留 | `data/master/`、`data/users/`、`accounts.json`、`user_mapping.json` | 迁移用 `--keep-sources` 保留在原处（程序已不读取），确认无误后可手工归档清理 | 待用户决定 |
| 已废弃脚本 | `scripts/prepare_master.py` | 单库模式下不再需要，已加废弃标记 | 保留参考 |

---

## 11. 版本历史

| 版本 | 日期 | 核心变更 |
|------|------|---------|
| v1.0 | 2026-05-20 | 初始版本（背题+模拟考试） |
| v2.0 | 2026-05-25 | 新增错题本、配置管理 |
| v2.1 | 2026-05-27 | 专项训练、批量I/O、模拟考试按超类抽题 |
| v2.2 | 2026-05-29 | 错题库简化、基于累计统计入出本规则 |
| v2.3 | 2026-06-11 | 已学率Bug修复、巩固练习错题复盘、全页面缓存机制、首页重构、JSON→SQLite迁移 |
| v2.4 | 2026-06-28 | 助记助学、相关性分析、文档更新（SQLite+四层架构） |
| **v2.5** | **2026-09-24** | **单库多租户改造**：全系统单一数据库 + `owner_id`/`user_id` 隔离；题库分层（公共/私人）；题目 ID 稳定化 + 导入 upsert 配对（内容指纹/位置/文本相似度三级）；身份入库（`users`/`user_devices`）+ 内置管理员 admin；`app_config`/`user_config` 配置分流；复合主键与草稿主键带 user_id；`EXMSYS_DATA_DIR` 支持；数据迁移脚本 + 内容级校验 + 4 套自动化测试 |
