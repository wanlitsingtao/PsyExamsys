# 心理咨询师考试——背题系统 需求说明书与技术方案

> **文档版本**：v2.2
> **更新日期**：2026-05-29
> **项目名称**：心理咨询师考试背题系统
> **题库来源**：E:\LingMa\exmsys\exmbase（11个docx文件，共1878题）
> **题型分布**：单选题1223题、多选题435题、判断题220题（含不定项选择题）
> **开发语言**：Python 3.14
> **前端框架**：Streamlit
> **数据存储**：JSON文件（本地持久化）

---

# 第一部分：需求说明书

## 1. 项目概述

### 1.1 项目背景
题库文件位于 `E:\LingMa\exmsys\exmbase` 目录下，包含11个docx格式的考题文件，覆盖心理咨询师考试的基础理论和咨询实务两大模块，含单选题、多选题、判断题、不定项选择题四种题型，总计1878道题目。需要基于这些题目构建一个功能完整的背题系统。

### 1.2 项目目标
开发一套包含**专项训练、模拟考试、错题本、数据统计、配置管理**五大模块的心理咨询师考试辅助学习系统，帮助用户高效备考。

### 1.3 用户角色
- **普通用户（考生）**：使用专项训练、模拟考试、错题本、数据统计功能
- **管理员**：使用配置管理模块进行题库管理和系统参数配置

---

## 2. 功能模块详细设计

### 2.1 模块总览

| 模块名称 | 功能描述 | 用户角色 |
|---------|---------|---------|
| 专项训练 | 综合训练+11个知识板块专项训练，统一提交模式 | 普通用户 |
| 模拟考试 | 两科固定结构模拟考（心理学综合+咨询实务），按超类抽题，定时计分 | 普通用户 |
| 错题本 | 错题解析+做错题双入口，基于累计统计的智能错题管理 | 普通用户 |
| 数据统计 | 题库/错题/答题/考试/学习五维统计面板 | 普通用户 |
| 配置管理 | 题库管理（导入/去重/导出）、系统参数配置、数据备份 | 管理员 |

---

### 2.2 专项训练模块

#### 2.2.1 功能描述
提供**综合训练**和**知识板块专项训练**两种模式。综合训练从全部题库中随机抽取60题（默认30单选+20多选+10判断）；知识板块专项训练从用户选择的特定板块（如"心理学导论"）中抽取相同数量题目。采用统一提交模式（全部答完后一次性提交），答题过程中可随时查看答案和解析，支持"再做一次"和"返回"操作。

#### 2.2.2 界面设计

**开始界面**：
```
┌─────────────────────────────────────────────────────┐
│  🎯 专项训练                                         │
│  ┌────────────────────┐ ┌────────────────────┐      │
│  │ 📚 综合训练        │ │ 📖 心理学导论       │      │
│  │  总题数: 1878      │ │  总题数: 250        │      │
│  │  已答: 120         │ │  已答: 32           │      │
│  │  答对: 96  答错: 24│ │  答对: 26  答错: 6  │      │
│  │  [开始综合训练]    │ │  [开始训练]        │      │
│  └────────────────────┘ └────────────────────┘      │
│  ...（共11个知识板块 + 1个综合训练卡片）             │
└─────────────────────────────────────────────────────┘
```

**答题界面**（统一提交模式）：
```
┌─────────────────────────────────────────────────────┐
│  🎯 专项训练 — 心理学导论                            │
│  ┌─────────────────────────────────────────────────┐ │
│  │  第 5/60 题   题型：单选题                        │ │
│  │  5. 下列哪项不属于心理学主要研究领域？（  ）。     │ │
│  │  🟢 上次答对                                    │ │
│  │                                                  │ │
│  │  ☐ A：认知心理学                                │ │
│  │  ☐ B：发展心理学                                │ │
│  │  ☐ C：临床心理学                                │ │
│  │  ☐ D：天体物理学                                │ │
│  │                                                  │ │
│  │  [◀ 上一题]  [📖 查看答案 ▼]  [下一题 ▶]         │ │
│  │  ┌──────────────────────────────────────────┐    │ │
│  │  │ 正确答案：D                              │    │ │
│  │  │ 天体物理学属于物理学领域，不属于心理学   │    │ │
│  │  │ 研究范畴。                               │    │ │
│  │  └──────────────────────────────────────────┘    │ │
│  └─────────────────────────────────────────────────┘ │
│  [提交全部答案]                                      │
│  答题卡导航：                                        │
│  🟢=答对  🔴=答错  🟡=已选  ⬜=未答  🔵=当前       │
└─────────────────────────────────────────────────────┘
```

**成绩报告界面**：
```
┌─────────────────────────────────────────────────────┐
│  📊 训练结果 — 心理学导论                            │
│  正确率: 80.0% (48/60)                               │
│                                                      │
│  分题型统计:                                        │
│  单选题: 25/30 (83.3%)                               │
│  多选题: 14/20 (70.0%)                               │
│  判断题: 9/10 (90.0%)                                │
│                                                      │
│  ❌ 错题回顾（每题可展开查看解析）:                  │
│  第 8 题 [单选题] — 你的答案：B，正确答案：C          │
│  天体物理学属于物理学领域...                         │
│                                                      │
│  [再做一次] [返回]                                   │
└─────────────────────────────────────────────────────┘
```

#### 2.2.3 界面元素详细说明

| 界面元素 | 类型 | 说明 |
|---------|------|------|
| 知识板块卡片 | 按钮卡片 | 显示板块名称、总题数、已答/答对/答错统计，题目不足时禁用 |
| 上次答题标记 | 文本标签 | 🟢 上次答对 / 🔴 上次答错（从 question_stats 读取 last_correct 字段） |
| 查看答案按钮 | Toggle按钮 | 点击展开/收起正确答案和解析（无需提交即可查看） |
| 答题卡导航 | HTML网格 | 底部色块快速跳转面板，颜色标识答题状态 |
| 返回按钮 | Button | 答题过程中可随时返回开始页面 |
| 提交全部答案按钮 | Button | 提交所有已选答案，触发评分和错题记录 |
| 成绩报告 | 卡片组合 | 正确率、分题型统计、每题详情（错题默认展开，含完整解析） |

#### 2.2.4 交互逻辑

1. **开始训练**：选择综合训练或指定知识板块，系统按优先级抽题算法抽取60题（30单选+20多选+10判断），按单选→多选→判断顺序排列
2. **答题过程**：
   - 可任意切换题目（上一题/下一题/答题卡点击跳转）
   - 答题卡实时更新颜色：黄色=已选未提交、蓝色边框=当前题
   - 随时点击"查看答案"展开/收起正确答案和解析
   - 显示该题历史答题统计和上次答题结果标记
3. **提交答案**：
   - 点击"提交全部答案"，使用批量操作一次性评分并更新数据
   - 多选题和不定项选择题：必须完全匹配才得分（漏选、多选均不得分）
   - 未答题目按错误处理
4. **成绩计算**：
   - 单选题每题1分，多选题每题1.5分，判断题每题0.5分
   - 满分为 30×1.0 + 20×1.5 + 10×0.5 = 65分
5. **结果记录**（批量写入，减少I/O）：
   - 答错题自动加入错题库（遵循错题统计规则）
   - 答对题从错题库移除（如符合移除条件）
   - 更新每题累计答对/答错次数统计（question_stats.json）
   - 追加答题过程记录（answer_records.json）

---

### 2.3 模拟考试系统模块

#### 2.3.1 功能描述
模拟真实考试环境，采用**固定两科结构**（不可配置）：

| 科目 | 时间 | 单选题 | 多选题 | 判断题 | 不定项选择题 | 满分 |
|------|------|--------|--------|--------|-------------|------|
| 心理学综合 | 120分钟 | 150题(0.4分/题) | 50题(0.6分/题) | 50题(0.2分/题) | 0题 | 100分 |
| 咨询实务 | 120分钟 | 140题(0.4分/题) | 60题(0.6分/题) | 0题 | 10题(0.8分/题) | 100分 |

两科按顺序依次进行（心理学综合→咨询实务），每科独立计时。题目从对应超类的知识板块中通过均衡采样算法抽取，确保各子板块均匀分配配额。

#### 2.3.2 界面设计

**考试界面**：
```
┌─────────────────────────────────────────────────────┐
│  📝 模拟考试 — 心理学综合  ⏱️ 剩余时间：01:55:30    │
│  ┌─────────────────────────────────────────────────┐ │
│  │  第 15/250 题   题型：多选题（0.6分）            │ │
│  │  151. 心理学研究的内容包括（  ）。              │ │
│  │                                                  │ │
│  │  ☑ A：心理现象                                 │ │
│  │  ☐ B：心理活动发生、发展的生理基础和机制        │ │
│  │  ☑ C：人类的生理结构                           │ │
│  │  ☐ D：制约心理现象的因素                       │ │
│  │                                                  │ │
│  │  [上一题]  [下一题]                             │ │
│  └─────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────┐ │
│  │ 答题卡:                                          │ │
│  │  01🟢 02🔴 03🟢 04🟢 05⬜ 06🟢 07⬜ 08🔴 ... │ │
│  │  [提交本科目试卷]                                │ │
│  └─────────────────────────────────────────────────┘ │
│  已答: 67/250  未答: 183/250                         │
│  [返回首页]                                          │
└─────────────────────────────────────────────────────┘
```

**成绩报告界面**：
```
┌─────────────────────────────────────────────────────┐
│  📊 模拟考试成绩报告                                 │
│                                                      │
│  科目一：心理学综合                                   │
│  📊 答题统计                                         │
│  总题: 250 | 答对: 206 | 答错: 44 | 正确率: 82.4%    │
│  得分: 82.4/100分                                    │
│                                                      │
│  科目二：咨询实务                                     │
│  （同理...）                                         │
│                                                      │
│  ❌ 错题回顾（全部错题，每题可展开查看解析）:         │
│  ┌──────────────────────────────────────┐           │
│  │ 第8题 [单选题] ❌                     │           │
│  │ 你的答案：B | 正确答案：C             │           │
│  │ 解析内容...                           │           │
│  └──────────────────────────────────────┘           │
│  [返回首页]                                          │
└─────────────────────────────────────────────────────┘
```

#### 2.3.3 交互逻辑

1. **开始考试**：点击"开始模拟考试"→确认弹窗→开始计时→进入心理学综合科目
2. **答题过程**：
   - 可任意切换题目（上一题/下一题/答题卡点击跳转）
   - 已答题在答题卡上标记绿色，当前题蓝色边框高亮
   - 倒计时持续显示，<10分钟黄色警告、<5分钟红色警告
3. **提交试卷**（批量处理）：
   - 提交本科目试卷→确认弹窗→自动进入下一科目或显示总成绩
   - 未答题目按错误处理
   - 使用批量操作（batch_update_wrong_and_stats）一次性更新错题库和统计数据
   - 250题提交从500+次I/O降至6次I/O，性能提升近百倍
4. **成绩计算**：
   - 每科独立计分，按题型权重计算
   - 显示分科目正确率、得分、及各知识板块分析
5. **错题回顾**：
   - 显示全部错题（非仅前10题）
   - 错题点击可展开查看完整题目、选项（含颜色标记）、正确答案和解析

---

### 2.4 错题本模块

#### 2.4.1 功能描述
提供**错题解析**和**做错题**两种模式。错题解析模式以左右布局展示错题列表和详细解析；做错题模式从错题库中提取最多50道题进行统一提交训练。错题库以纯题号列表（qid数组）形式存储，所有统计数据从 question_stats.json 读取。

#### 2.4.2 错题判断规则

**核心规则**（v2.2）：
- 错题判定依据 `question_stats.json` 中的累计统计数据（`correct_count`/`wrong_count`），而非单独维护的 `wrong_count` 字段
- 入错题本条件：`wrong_count >= correct_count` **且** 至少答过一次（`wrong_count > 0` 或 `correct_count > 0`）
- 出错题本条件：`correct_count > wrong_count`（即答对次数严格大于答错次数）
- 从未答过的题目（`wc=0, cc=0`）不会进入错题本

**与旧规则的关键区别**：
| 维度 | 旧规则 (v2.1) | 新规则 (v2.2) |
|------|-------------|-------------|
| 入本条件 | 本次答错即加入 | 累计答错次数 >= 累计答对次数 |
| 出本条件 | 本次答对即移除 | 累计答对次数 > 累计答错次数 |
| 数据存储 | 错题库自维护 wrong_count | 错题库仅存qid列表，统计从 question_stats.json 读取 |
| wc=0,cc=0 | 可能错误加入 | 确保不会加入 |

#### 2.4.3 界面设计

```
┌─────────────────────────────────────────────────────┐
│  📕 错题本 (135)                                      │
│  ┌─────────────────────────────────────────────────┐ │
│  │  [📖 错题解析]  [📝 做错题]                     │ │
│  └─────────────────────────────────────────────────┘ │
│                                                      │
│  ═══════════ 错题解析模式 ═══════════               │
│  ┌──────────────┐  ┌──────────────────────────────┐ │
│  │ 错题列表     │  │ 题目详情 & 解析               │ │
│  │ 1. 单选题    │  │ 第X题 [单选题]                │ │
│  │ 答错3次 ✅1  │  │ 题目内容...                   │ │
│  │ 2. 多选题    │  │ 选项A/B/C/D（含颜色标记）      │ │
│  │ 答错5次 ✅2  │  │ 正确答案：C                   │ │
│  │ ...          │  │ 解析：...                     │ │
│  │ [清空错题本] │  │ [我已掌握本题]  [重新组卷]    │ │
│  └──────────────┘  └──────────────────────────────┘ │
│                                                      │
│  ═══════════ 做错题模式 ═══════════                 │
│  同专项训练的统一提交模式，从错题库抽取               │
└─────────────────────────────────────────────────────┘
```

#### 2.4.4 交互逻辑

1. **错题解析模式**：左侧按易错程度排序的题目列表，点击后在右侧显示完整题目、选项、正确答案和解析
2. **做错题模式**：从错题库按优先级排序抽取最多50题，统一提交答题模式
3. **我已掌握本题**：手动将某题从错题本移除（仅当 `correct_count > wrong_count` 时生效）
4. **清空错题本**：二次确认后清空所有错题记录
5. **错题统计**：所有统计数据从 `question_stats.json` 读取（易错程度按 `wrong_count - correct_count` 降序排列）

---

### 2.5 数据统计模块

#### 2.5.1 功能描述
提供五维数据统计面板，帮助用户全面了解学习进度和薄弱环节。

#### 2.5.2 五个统计维度

| Tab标签 | 内容 |
|---------|------|
| 📚 题库统计 | 总题数、各题型分布、知识板块分布（表格+条形图）、来源文件统计 |
| 📕 错题统计 | 错题总数、最高答错次数、错题占比、知识板块错题分布、高频错题TOP 20 |
| 📝 答题统计 | 总答题次数、正确率、各模式统计（专项/模拟/错题本）、各板块答题统计、最近50条答题记录 |
| 📊 考试记录 | 模拟考试记录（按会话分组显示两科成绩、正确率、用时） |
| 📅 学习记录 | 训练会话数、每日学习量（折线图） |

#### 2.5.3 交互逻辑
- 各Tab独立加载，切换Tab时动态查询最新数据
- 答题记录自动从 `answer_records.json` 读取
- 错题统计数据从 `question_stats.json` 和 `wrong_questions.json` 聚合计算
- 统计结果实时反映当前数据状态

---

### 2.6 配置管理模块

#### 2.6.1 功能描述
管理员管理题库和系统参数配置。包含两个Tab：**题库管理**和**系统设置**。

#### 2.6.2 界面设计

```
┌─────────────────────────────────────────────────────┐
│  ⚙️ 配置管理                                        │
│  ┌─────────────────────────────────────────────────┐ │
│  │  [📚 题库管理]  [⚙️ 系统设置]                   │ │
│  └─────────────────────────────────────────────────┘ │
│                                                      │
│  ═══════════ 题库管理 ═══════════                   │
│  - 题库统计展示（各题型数量、知识板块分布）           │
│  - 导入目标题库选择（4种题库类型切换）                │
│  - 批量导入 exmbase 目录所有 docx                    │
│  - 上传 docx 文件导入                                │
│  - 智能去重（基于MD5哈希）                            │
│  - 导出题库为JSON                                    │
│  - 清空题库（二次确认）                               │
│                                                      │
│  ═══════════ 系统设置 ═══════════                   │
│  - 专项训练题数配置（总数/单选/多选/判断）            │
│  - 模拟考试设置（时间/各题型数）                      │
│  - 错题本提取数                                       │
│  - 手动数据备份                                       │
│  [保存设置]                                          │
└─────────────────────────────────────────────────────┘
```

#### 2.6.3 交互逻辑

1. **题库管理**：
   - 显示已导入题库文件列表及统计
   - 支持4种题库类型（心理学会四级/三级、心理协会四级/三级）切换
   - 导入时MD5去重，自动跳过重复题
   - 清空题库需二次确认
2. **系统设置**：
   - 专项训练每轮题量配置（默认总数60）
   - 模拟考试时间（默认90分钟，实际考试使用固定两科配置）
   - 错题本提取数量（默认50，可调10~100）
   - 支持手动数据备份（一键备份所有JSON文件到 backup/ 目录）

---

## 3. 非功能性需求

### 3.1 性能要求
- 题库加载时间不超过3秒
- 题目切换响应时间不超过0.5秒
- 模拟考试倒计时精度在1秒以内
- 题库导入解析速度：单文件不超过5秒
- 模拟考试提交（250题）：使用批量操作，6次I/O即可完成（v2.2优化）
- 专项训练结果展示：答题统计缓存于 session_state，避免重复I/O（v2.2优化）

### 3.2 可靠性要求
- 考试中途意外关闭后状态不丢失（Session State 内数据仍保留）
- 数据文件定期自动备份（每日首次启动）
- 导入中断不破坏已有题库数据
- JSON文件损坏时自动重建空数据结构，不影响应用启动

### 3.3 易用性要求
- 所有交互操作有明确反馈提示
- 重要操作（提交试卷、清空错题/题库）有二次确认
- 响应式布局，适配不同屏幕尺寸
- 错题本导航栏显示错题数量角标，如 "📕 错题本 (135)"

### 3.4 数据持久化
- 所有数据以JSON文件存储于 `E:\LingMa\exmsys\data\`
- 核心数据文件：questions.json、wrong_questions.json、question_stats.json、config.json、mock_exam_records.json、answer_records.json、study_records.json
- 每日首次启动自动备份数据文件到 `data/backup/`

---

## 4. 数据存储设计

### 4.1 题库数据结构
```json
{
  "id": "b25a3a66236d",
  "source_file": "【咨询实务1】心理咨询会谈技术.docx",
  "index": 1,
  "type": "single",
  "question": "会谈在心理咨询中的核心作用不包括（  ）。",
  "options": {
    "A": "建立咨询关系",
    "B": "制定个性化咨询方案",
    "C": "直接替来访者解决现实矛盾",
    "D": "评估来访者心理状态"
  },
  "answer": "C",
  "explanation": "出处：P4,原文明确会谈是...",
  "md5": "b25a3a66236d41e5f9896114ad48af1b",
  "category": "心理咨询会谈技术",
  "exam_type": "心理学会咨询师四级"
}
```
- `id`: MD5前12位，全局唯一标识
- `category`: 知识板块名称（11个具体板块之一）
- `exam_type`: 题库类型，支持4种题库分类

### 4.2 错题库数据结构 (v2.2 新格式)

```json
[
  "020920cc799c",
  "02631b85c794",
  "03ef6a1e3d3b"
]
```

**v2.2 重大变更**：错题库从对象列表格式 `[{question_id, wrong_count, wrong_records, ...}]` 简化为**纯题号ID列表**。
- 不再在错题库中维护 `wrong_count`、`wrong_records`、`last_wrong_time` 等冗余字段
- 所有答题统计数据统一从 `question_stats.json` 读取
- 提供 `_extract_qids_from_wrong_list()` 兼容函数，支持新旧两种格式的读取

**错题判断规则（v2.2）**：
- 入本条件：`wrong_count >= correct_count` 且至少答过一次
- 出本条件：`correct_count > wrong_count`
- 数据源：question_stats.json（唯一统计数据来源）

### 4.3 题目答题统计数据结构

```json
{
  "b25a3a66236d": {
    "correct_count": 3,
    "wrong_count": 5,
    "last_answer_time": "2026-05-29T13:40:00.000000",
    "last_correct": false
  }
}
```

- `correct_count`: 累计答对次数（累加，永不减少）
- `wrong_count`: 累计答错次数（累加，永不减少）
- `last_answer_time`: 最后一次答题时间（ISO格式）
- `last_correct`: 最后一次答题是否答对（v2.1新增，用于专项训练显示"上次答对/答错"标记）

### 4.4 配置数据结构

```json
{
  "study_per_round": 50,
  "study_single_count": 20,
  "study_multi_count": 10,
  "study_judge_count": 10,
  "spec_per_round": 60,
  "spec_single_count": 30,
  "spec_multi_count": 20,
  "spec_judge_count": 10,
  "exam_time_minutes": 90,
  "exam_single_count": 40,
  "exam_multi_count": 30,
  "exam_judge_count": 30,
  "wrongbook_extract_count": 50,
  "last_import_files": []
}
```

### 4.5 其他数据文件

| 文件 | 说明 |
|------|------|
| mock_exam_records.json | 模拟考试记录（按会话分组，含两科成绩） |
| answer_records.json | 答题过程全量记录（每次答题的题目、用户答案、结果） |
| study_records.json | 训练会话历史记录 |

---

# 第二部分：技术方案

## 1. 技术选型

| 技术 | 选型 | 说明 |
|-----|------|------|
| 编程语言 | Python 3.14 | 统一开发语言 |
| Web框架 | Streamlit 1.57 | 快速构建交互界面的Python框架 |
| 文档解析 | python-docx | 解析docx题库文件 |
| 数据存储 | JSON | 轻量级本地持久化 |
| 哈希去重 | hashlib.md5 | 题库导入智能去重 |
| 包管理 | uv | 快速依赖管理 |

---

## 2. 系统架构

```
┌────────────────────────────────────────────────────────────┐
│                    展示层 (Presentation)                    │
│  app.py ── 主控制器、路由分发（5个页面）                   │
│  pages/  ── specialized / mock_exam / wrongbook /          │
│             statistics / settings                          │
├────────────────────────────────────────────────────────────┤
│                    业务逻辑层 (Business Logic)               │
│  优先级抽题 / 均衡采样 / 答案判断 / 成绩计算 /             │
│  错题排序 / 倒计时管理 / 批量数据更新                     │
├────────────────────────────────────────────────────────────┤
│                    数据访问层 (Data Access)                  │
│  utils/parser.py       —— docx解析器                       │
│  utils/data_manager.py —— 数据CRUD + 去重 + 批量更新       │
├────────────────────────────────────────────────────────────┤
│                    数据存储层 (Storage)                     │
│  data/questions.json        （题库: 1878题）               │
│  data/wrong_questions.json  （错题库: 纯qid列表）          │
│  data/question_stats.json   （题目答题统计）               │
│  data/config.json           （系统配置）                   │
│  data/mock_exam_records.json（模拟考试记录）               │
│  data/answer_records.json   （答题过程记录）               │
│  data/study_records.json    （训练会话记录）               │
│  data/backup/               （自动备份目录）               │
│  exmbase/                   （原始docx源文件）              │
└────────────────────────────────────────────────────────────┘
```

## 3. 文件结构

```
E:\LingMa\exmsys\
├── app.py                            # 主入口（151行）
├── start.bat                         # Windows启动脚本（port 8510）
├── data\                             # 系统数据目录
│   ├── questions.json                # 解析后的题库（1878题）
│   ├── wrong_questions.json          # 错题库（纯qid列表，135条）
│   ├── question_stats.json           # 题目答题统计
│   ├── config.json                   # 系统配置
│   ├── mock_exam_records.json        # 模拟考试记录
│   ├── answer_records.json           # 答题过程全量记录
│   ├── study_records.json            # 训练会话记录
│   └── backup\                       # 自动备份目录
├── utils\                            # 工具模块
│   ├── __init__.py
│   ├── parser.py                     # docx题库解析器（209行）
│   └── data_manager.py              # 数据管理器（1127行）
├── pages\                            # 页面模块（5个活跃 + 3个遗留）
│   ├── specialized.py               # 专项训练（709行）★活跃
│   ├── mock_exam.py                  # 模拟考试（757行）★活跃
│   ├── wrongbook.py                  # 错题本（579行）★活跃
│   ├── statistics.py                 # 数据统计（502行）★活跃
│   ├── settings.py                   # 配置管理（304行）★活跃
│   ├── home.py                       # [遗留] 旧版首页
│   ├── exam.py                       # [遗留] 旧版综合训练
│   └── study.py                      # [遗留] 旧版背题系统
├── exmbase\                          # 原始docx题库（11个文件）
├── docs\                             # 文档目录
│   ├── 需求说明书.md
│   ├── 设计说明书.md
│   ├── bugfix_v2.1_20260527.md
│   └── bugfix_v2.2_20260529.md
└── start.bat                         # 启动脚本
```

## 4. 核心模块设计

### 4.1 docx题库解析器 (parser.py)

解析规则（逐行扫描docx段落）：
1. 匹配 `^(\d+)\.\s` 开头 → 题目行
2. 题目行中检测 `(单选题|多选题|判断题|不定项选择题)` → 确定题型
3. 后续段落匹配 `^[A-Z][：:]` → 选项
4. 匹配 `正确答案：` → 答案
5. 匹配 `答案解析：` → 解析
6. MD5哈希生成（题目+选项+答案），取前12位作为唯一ID

### 4.2 数据管理器 (data_manager.py)

**核心函数（1127行）**：

**题库管理**：`load_questions()` / `save_questions()` / `dedup_import()` / `get_all_categories()`

**抽题算法**：
- `extract_questions()` — 按题型随机抽样
- `extract_questions_by_category()` — 按知识板块抽题
- `extract_questions_by_super()` — 按超类（基础理论/咨询实务）抽题
- `_priority_sample()` — 优先级排序抽题（错题优先>新题>旧题）
- `_balanced_sample_by_type()` — 均衡配额分配（确保各子板块均匀出题）

**错题管理（v2.2）**：
- `add_wrong_record(qid)` — 错题记录（基于统计规则判断是否入本）
- `add_correct_record(qid)` — 答对记录（基于统计规则判断是否出本）
- `get_top_wrong_questions(count)` — 按易错程度降序取题
- `remove_wrong_question(qid)` — 从错题本移除
- `clear_all_wrong()` — 清空错题本
- `_extract_qids_from_wrong_list()` — 兼容新旧格式读取

**批量操作（v2.1/v2.2 性能优化）**：
- `batch_update_wrong_and_stats()` — 批量更新错题库和答题统计（单次读写）
- `batch_add_answer_records()` — 批量追加答题过程记录

**统计查询**：
- `get_question_stats(qid)` — 获取单题答题统计
- `update_question_stats(qid, is_correct)` — 更新答题统计
- `get_wrong_stats()` — 错题聚合统计
- `get_all_wrong_with_stats()` — 错题列表+统计合并查询

**配置管理**：`load_config()` / `save_config()`

**备份**：`backup_data()` — 一键备份所有数据文件

### 4.3 优先级抽题算法（v2.2）

```python
def _priority_sample(questions, count):
    """
    三层排序优先级：
    1. 主排序：wrong_count - correct_count 降序（差值越大越优先）
    2. 副排序：答错过(0) > 从未答过(1) > 答对过(2)
    3. 同层级内：wrong_count 降序
    4. 最终平局：random 随机打破
    """
```

### 4.4 均衡采样算法（v2.1）

```python
def _balanced_sample_by_type(questions_by_category, total, qtype):
    """
    从各子类别中均匀分配配额：
    1. 基础配额 = total // n（n为子类别数）
    2. 前 extra 个类别多1道
    3. 配额未用完时从剩余题目中补齐
    确保大板块不会挤压小板块的出题数
    """
```

### 4.5 答案判断逻辑

```python
def check_answer(question_type, user_answer, correct_answer):
    if question_type in ("single", "judge"):
        return user_answer.strip().upper() == correct_answer.strip().upper()
    elif question_type in ("multi", "indefinite"):
        user_set = set(user_answer.strip().upper().replace(" ", ""))
        correct_set = set(correct_answer.strip().upper().replace(" ", ""))
        return user_set == correct_set  # 完全匹配才得分
```

---

## 5. 开发计划

| 阶段 | 任务 | 预估工时 |
|------|------|---------|
| 准备 | 安装依赖、创建项目结构 | 0.5天 |
| 阶段1 | 开发题库解析器（parser.py）+ 数据管理器（data_manager.py） | 1天 |
| 阶段2 | 开发专项训练页面（specialized.py，含综合+板块训练） | 1天 |
| 阶段3 | 开发模拟考试页面（mock_exam.py，两科固定结构） | 1天 |
| 阶段4 | 开发错题本页面（wrongbook.py）+ 数据统计页面（statistics.py） | 0.5天 |
| 阶段5 | 开发配置管理页面（settings.py） | 0.5天 |
| 阶段6 | 主程序集成（app.py）、菜单导航 | 0.5天 |
| 阶段7 | 测试、优化、文档编写 | 0.5天 |
| **合计** | | **5天** |

---

## 6. 部署说明

### 6.1 环境要求
- Python 3.10+
- Windows 10/11 操作系统
- 磁盘空间：100MB+

### 6.2 安装步骤
```bash
cd E:\LingMa\exmsys
uv venv
uv pip install streamlit python-docx
streamlit run app.py --server.port 8510
# 或双击 start.bat
```

### 6.3 依赖清单
```
streamlit>=1.30.0
python-docx>=1.1.0
```

---

## 7. 扩展性设计

### 7.1 未来可扩展功能
- **用户系统**：多用户登录，各自独立错题本和学习记录
- **云端同步**：支持数据同步到云端，跨设备使用
- **智能组卷**：根据错题分布，智能加强薄弱环节的出题比例
- **移动端适配**：Streamlit 天然支持移动端浏览器

### 7.2 数据迁移方案
- JSON → SQLite/Supabase 只需替换 DataManager 的数据访问接口
- 错题库格式兼容：`_extract_qids_from_wrong_list()` 同时支持新旧格式
